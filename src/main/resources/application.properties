spring.application.name=watchdog

server.port=${SERVER_PORT:8080}

management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

alert.email.override=${ALERT_EMAIL_OVERRIDE:}
